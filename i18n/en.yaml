readingTime:
  one: "{{ .Count }} minute read"
  other: "{{ .Count }} minutes read"

readingTimeHours:
  one: "{{ .Count }} hour read"
  other: "{{ .Count }} hours read"

readingTimeHoursAndMinutes:
  one: "{{ .Hours }} hour {{ .Minutes }} minutes read"
  other: "{{ .Hours }} hours {{ .Minutes }} minutes read"
